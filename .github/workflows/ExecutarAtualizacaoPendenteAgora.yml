name: Executar diagnóstico das codificações agora

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/ExecutarAtualizacaoPendenteAgora.yml"
  workflow_dispatch:

permissions:
  actions: write
  contents: read

jobs:
  acionar:
    runs-on: ubuntu-latest
    steps:
      - name: Iniciar o diagnóstico da base enviada
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          gh api \
            --method POST \
            -H "Accept: application/vnd.github+json" \
            "repos/$GITHUB_REPOSITORY/actions/workflows/DiagnosticarCodificacoesDaBase.yml/dispatches" \
            -f ref=main
          echo "Diagnóstico das codificações solicitado."
